# === App ===
SECRET_KEY=genera-una-cadena-larga-aleatoria
APP_NAME=Agenda Facturas Perú
APP_URL=https://agenda.erpgestapp.com
CRON_SECRET=genera-otro-secreto-para-cron

# === Postgres (también usa docker-compose) ===
POSTGRES_DB=agenda_facturas
POSTGRES_USER=agenda
POSTGRES_PASSWORD=genera-password-fuerte
# DATABASE_URL la inyecta docker-compose; no hace falta definirla en runtime
# salvo que corras la app fuera de Docker.

# === SMTP (opcional) ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=Agenda Facturas <avisos@erpgestapp.com>

# === Web Push (recomendado reutilizar las de Railway) ===
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
VAPID_CLAIM_EMAIL=mailto:avisos@erpgestapp.com

# === Opcional ===
SUNAT_API_TOKEN=
TELEGRAM_BOT_TOKEN=
